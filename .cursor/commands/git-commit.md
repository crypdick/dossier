`git add` the files you changed, and then `git commit`. split it into multiple commits if it makes sense to.

If the changes are substantial, run @pytest-integration.md to verify everything still works before committing.
